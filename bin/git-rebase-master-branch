#!/usr/bin/env bash

BRANCH=$1

if [ -z "$BRANCH" ]; then
  BRANCH="master"
fi

printf "RUN :: rebase on the current branch with $BRANCH"

git checkout $BRANCH
git pull origin $BRANCH
git checkout -
git rebase $BRANCH
